---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: jsonservers.example.com
spec:
  group: example.com
  names:
    kind: JsonServer
    listKind: JsonServerList
    plural: jsonservers
    singular: jsonserver
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: JsonServer is the Schema for the JsonServers API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: JsonServerSpec defines the desired state of JsonServer.
            properties:
              jsonConfig:
                description: JsonConfig represents config passed to be processed by
                  JsonServer
                minLength: 1
                type: string
              replicas:
                description: Replicas are a number of instances of JsonServer
                format: int32
                minimum: 0
                type: integer
            required:
            - jsonConfig
            type: object
          status:
            description: JsonServerStatus defines the observed state of JsonServer.
            properties:
              message:
                description: Message regarsing state
                type: string
              state:
                description: Current State of JsonServer
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      scale:
        labelSelectorPath: .status.selector
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
      status: {}
