apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: json-server-controller
    app.kubernetes.io/managed-by: kustomize
  name: webhook-service
  namespace: json-server-controller
spec:
  ports:
    - port: 443
      protocol: TCP
      targetPort: 9443
  selector:
    control-plane: controller-manager
    app.kubernetes.io/name: json-server-controller
